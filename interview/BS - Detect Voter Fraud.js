function solve(votes) {
    // check if array votes less than or equal to zero in length, return false
    if (votes.length === 0) return false;

    
    
}

let arr = [];
arr = [
    [3, 1],
    [3, 0],
    [3, 4],
    [3, 3],
    [3, 2]
]


const res = solve(arr);
console.log(res);