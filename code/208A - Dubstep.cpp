#include <bits/stdc++.h>
using namespace std;
#define out cout <<
#define in cin >>

int main(void) {
    string m;
    in(m);
    int stringLength = input.length();
    
    m.erase();


    
    while (i < stringLength) {
        
        if (m[i] == 'W' && m[i+1] == 'U' && m[i+2] == 'B') {
            i += 3;
            
            
        }


    }

    
}